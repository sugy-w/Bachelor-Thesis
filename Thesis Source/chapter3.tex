\chapter{Predictive analyses on IT segment}

This chapter presents obtained modelling results. For each of the models used, in-depth parameter tuning and evaluation 
on both the training-validation and test datasets are performed. 

\section{Evaluation metrics}

Throughout the thesis, particular metrics of goodness of regression are used. Suppose that on the day $t$
we acquired closing price data $Y_t$; therefore, based on the methodology introduced earlier (Subchapter \ref{rolling}),
we obtain a prediction for the day $t+1$ as $\hat{Y}_{t+1}$.

Firstly, in order to measure deviation from the actual $Y_{t+1}$, metric \textbf{absolute percentage error} (APE) is used.
The percentage relativization was chosen in order to unify the scale among all the stocks, making the prediction error comparable
across the stocks and more interpretable. The complete definition of the metric is given as
\begin{equation*}
    \text{APE}(\hat{Y}_{t+1}, Y_{t+1})=\frac{Y_{t+1}-\hat{Y}_{t+1}}{Y_{t+1}}.
\end{equation*}
Building upon the APE definition, the average value of the APE for individual stocks over the entire time interval considered is evaluated.

Apart from that, we introduce a baseline benchmark to measure the model's significance. According to Fama, the asset price developments correspond to 
the random walk \cite{fama1970efficient}. In accordance with this, such naïve method can be introduced, that benchmark predicts the next-day price at the $t+1$
to be equal to the price on the day $t$.

With the benchmark method defined, we quantify performance as the proportion of predictions made by the model that are closer, in terms of APE, to the actual 
closing price than the predictions made by the naïve benchmark. From now on, such benchmarking will be referred as the \textbf{model's dominance}.

\section{Linear regression}
Although linear regression might seem rather simple, compared to other conventional ML frameworks it has been implemented in financial predictive
modelling quite regularly. According to Zhao \cite{zhao}, linear regression is capable of reflecting the trend-based development of a certain stock up to a certain extent. 
Zhao suggests that linear regression alone might not reflect patterns in general for any stock.

On the other hand, Wang et al. \cite{wang} suggest that linear regression, with appropriate data preprocessing, is capable of performing short-term predictions up to a certain
accuracy threshold.

Our next-day approach can be considered short-term; therefore, we decided to include this framework in the thesis. Apart from the conventional regression, we considered 
a possible feature selection optimization in each rolling window step and optimized the parameter $p$.

\subsection{Possible feature selection}
In order to analyze the possible contribution of omitting certain predictors, we implemented rolling forecasting with the selection of given $k$ most relevant predictors
with respect to the next-day closing price. Taking into account the potentially extensive computational time of such prediction, predictor selection based on $F$-statistics
was used. 

The statistics reflect the ratio of variance explained by a single predictor (with respect to the closing price) to the remaining unexplained variance. Predictors that achieve higher values of the statistics
are considered strongly correlated with the target \cite{islp}. After such precomputation, the $k$ best-performing predictors are kept in feature matrix.

However, such an approach tends not to perform 
significantly better than keeping all the predictors. Figure \ref{fig:3.1} shows a sample example of MAPE dependence on the number of selected best features for the stock Enphase Energy Inc. (ENPH).  

It is possible to observe that omitting less correlated predictors causes and increase in MAPE. Up to a certain number $k$, the MAPE level tends to decrease. A further decrease in $k$ results in a stable MAPE level (red dotted line),
which represents the level of MAPE when no predictors are omitted.

Such behaviour was observed in the vast majority of stocks analyzed, with only slight (roughly $10^{-1} - 10^{-3}\%$) deviations below the benchmark line were observed. Furthermore, no feature selection in terms of linear regression 
is used.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_fs.png}
    \caption{\centering MAPE dependence on number of features selected (stock ENPH).}
    \label{fig:3.1}
\end{figure}
\subsection{Optimization of $p$}
The only a priori model parameter present in the linear regression is the number of observations included in the training (denoted as $p$). Analysis of the dependence of MAPE on the parameter $p$
for the selected ENPH stock, is shown in Figure \ref{fig:3.2}. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_mape.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter (stock ENPH).}
    \label{fig:3.2}
\end{figure}

In Figure \ref{fig:3.2}, local minimum is observed at $p=4$. In most of the included stocks we observed a similar convex development of MAPE dependence on $p$. What is more,
the minimum, in the vast majority of stocks, is observed in $p=4$, rarely at $p=5$. The value of $p=4$ was chosen, based on the optimization on the training-validation 
interval.

Such a defined model achieved (on the training-validation interval) MAPE of $\textbf{1.844 \%}$ and dominance of $\textbf{44.82 \%}$ on average for all the stocks. In terms of interpretation,
this means that linear regression is inferior, in terms of prediction accuracy, to the naïve benchmark on the given interval.

It is possible to analyze performance of the model for the given set of stocks. In Figure \ref{fig:3.5} is the overview of distributions of percentual deviations of predictions on the training-validation interval.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.70\linewidth]{images/distr_lr.png}
    \caption{\centering Errors in prediction for single stocks (linear regression).}
    \label{fig:3.5}
\end{figure}
In the vast majority of stocks, maximum error of $20 \%$ was observed; however even pathological predictions with abnormally high error was suspected. Even though, these cases were observed
in minimal number of cases – average percentual error per stock did not exceed $4 \%$. 

\section{Lasso regularization}

Introducing regularization into the regression process might presumably improve the overall performance, in terms 
of the introduced metrics, even though explicit feature selection has not proved to be any sufficient. Apart from the linear regression,
it is necessary to further tune the regularization parameter $\lambda$, together with the length of training set $p$.

Figure \ref{fig:3.3} visualizes performance of the MAPE depending on the chosen parameters $\lambda, p$ for the ENPH stock. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_lambda.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter and regularization parameter (stock ENPH).}
    \label{fig:3.3}
\end{figure}
In terms of the parameter $p$, it is possible to observe that the lowest MAPE is being achieved in approximate interval $p \in [4, 8]$ for chosen values of $\lambda$.
Based on further evaluation of mean MAPE performance for all the stocks, the lowest mean MAPE was achieved for $p=4$. 

Further, the interval of the optimal setting of $\lambda$ for the given stocks as $\lambda \in [4, 5]$. Figure \ref{fig:3.4} depicts the overview of MAPE for the chosen ENPH stock.
What needs to be pointed out is the MAPE fluctuation on the given subinterval $[4.5, 4.7]$. The improvement remains negligible, reaching at most the magnitude of $10^{-3} - 10^{-2} \%$. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/lambda_lasso.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter and regularization parameter (stock ENPH).}
    \label{fig:3.4}
\end{figure}
With the optimized parameters we decided to verify two approaches in setting the model. First, we used optimized values of $\lambda$ of each of the stocks. The resulting value 
is $\overline{\lambda}=4.15$. We set up the Lasso model with the given $\overline{\lambda}$ (General model) and, in contrast, we verified performance of model,
which checks which stock is being predicted and fetches particular stock's optmized $\lambda$ (Stock-wise model). The results of performance on the given training-validation interval
are overviewed in the Table \ref{tab:mape_lambda}.
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{Model Type} & \textbf{MAPE (\%)} & \textbf{Dominance (\%)} \\
\hline
General model & 2.297 & 36.16 \\
\hline
Stock-wise model & 2.27 & 35.81 \\
\hline
\end{tabular}
\caption{MAPE and dominance values for different settings of the model.}
\label{tab:mape_lambda}
\end{table}

Generally speaking, compared to the linear regression there is a marginal downfall in terms of both metrics. What is interesting is the behaviour of both metrics. General model has achieved comparatively worse MAPE; however,
its dominance is little over the dominance of the stock-wise model.

Lastly, Figure \ref{fig:3.6} visualizes performance of the optimized model for single stocks. The visualization shows performance of general model
as there is no marginal difference in the particular visualisation of stock-wise model. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/distr_lasso.png}
    \caption{\centering Errors in prediction for single stocks (General model Lasso).}
    \label{fig:3.6}
\end{figure}
It is possible to suspect very similar distribution to the one in Figure \ref{fig:3.5}, visualizing distribution of error in linear regression.
Even though, there are a few minor differences – the order of the stocks, in terms of range of errors, is not preserved. For example, the lowest range in Lasso was achieved on AAPL stock (Apple Inc.); however,
in linear regression in stock IBM. This suggests that different ML algorithms most likely perform in various ways when predicting next-day price of single stocks – the stock having relatively the best results of predictions
does not have to perform the best in the other algorithm. 

% Pre čitateľnosť, keď píšem v pdf-ku
\newpage

\section{Ridge regularization}

\section{k-Nearest neighbours regression}

\section{Random forest regression}

\section{ARIMA regression}

