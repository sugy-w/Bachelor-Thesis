\chapter{Predictive analyses on IT segment}

This chapter presents obtained modelling results. For each of the models used, in-depth parameter tuning and evaluation 
on both the training-validation and test datasets are performed. 

\section{Evaluation metrics}

Throughout the thesis, particular metrics of goodness of regression are used. Suppose that on the day $t$
we acquired closing price data $Y_t$; therefore, based on the methodology introduced earlier (Subchapter \ref{rolling}),
we obtain a prediction for the day $t+1$ as $\hat{Y}_{t+1}$.

Firstly, in order to measure deviation from the actual $Y_{t+1}$, metric \textbf{absolute percentage error} (APE) is used.
The percentage relativization was chosen in order to unify the scale among all the stocks, making the prediction error comparable
across the stocks and more interpretable. The complete definition of the metric is given as
\begin{equation*}
    \text{APE}(\hat{Y}_{t+1}, Y_{t+1})=\frac{Y_{t+1}-\hat{Y}_{t+1}}{Y_{t+1}}.
\end{equation*}
Building upon the APE definition, the average value of the APE for individual stocks over the entire time interval considered is evaluated.

Apart from that, we introduce a baseline benchmark to measure the model's significance. According to Fama, the asset price developments correspond to 
the random walk \cite{fama1970efficient}. In accordance with this, such naïve method can be introduced, that benchmark predicts the next-day price at the $t+1$
to be equal to the price on the day $t$.

With the benchmark method defined, we quantify performance as the proportion of predictions made by the model that are closer, in terms of APE, to the actual 
closing price than the predictions made by the naïve benchmark. From now on, such benchmarking will be referred as the \textbf{model's dominance}.

\section{Linear regression}
Although linear regression might seem rather simple, compared to other conventional ML frameworks it has been implemented in financial predictive
modelling quite regularly. According to Zhao \cite{zhao}, linear regression is capable of reflecting the trend-based development of a certain stock up to a certain extent. 
Zhao suggests that linear regression alone might not reflect patterns in general for any stock.

On the other hand, Wang et al. \cite{wang} suggest that linear regression, with appropriate data preprocessing, is capable of performing short-term predictions up to a certain
accuracy threshold.

Our next-day approach can be considered short-term; therefore, we decided to include this framework in the thesis. Apart from the conventional regression, we considered 
a possible feature selection optimization in each rolling window step and optimized the parameter $p$.

\subsection{Possible feature selection}
In order to analyze the possible contribution of omitting certain predictors, we implemented rolling forecasting with the selection of given $k$ most relevant predictors
with respect to the next-day closing price. Taking into account the potentially extensive computational time of such prediction, predictor selection based on $F$-statistics
was used. 

The statistics reflect the ratio of variance explained by a single predictor (with respect to the closing price) to the remaining unexplained variance. Predictors that achieve higher values of the statistics
are considered strongly correlated with the target \cite{islp}. After such precomputation, the $k$ best-performing predictors are kept in feature matrix.

However, such an approach tends not to perform 
significantly better than keeping all the predictors. Figure \ref{fig:3.1} shows a sample example of MAPE dependence on the number of selected best features for the stock Enphase Energy Inc. (ENPH).  

It is possible to observe that omitting less correlated predictors causes and increase in MAPE. Up to a certain number $k$, the MAPE level tends to decrease. A further decrease in $k$ results in a stable MAPE level (red dotted line),
which represents the level of MAPE when no predictors are omitted.

Such behaviour was observed in the vast majority of stocks analyzed, with only slight (roughly $10^{-1} - 10^{-3}\%$) deviations below the benchmark line were observed. Furthermore, no feature selection in terms of linear regression 
is used.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_fs.png}
    \caption{\centering MAPE dependence on number of features selected (stock ENPH).}
    \label{fig:3.1}
\end{figure}
\subsection{Optimization of $p$}
The only a priori model parameter present in the linear regression is the number of observations included in the training (denoted as $p$). Analysis of the dependence of MAPE on the parameter $p$
for the selected ENPH stock, is shown in Figure \ref{fig:3.2}. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_mape.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter (stock ENPH).}
    \label{fig:3.2}
\end{figure}

In Figure \ref{fig:3.2}, local minimum is observed at $p=4$. In most of the included stocks we observed a similar convex development of MAPE dependence on $p$. What is more,
the minimum, in the vast majority of stocks, is observed in $p=4$, rarely at $p=5$. The value of $p=4$ was chosen, based on the optimization on the training-validation 
interval.

Such a defined model achieved (on the training-validation interval) MAPE of $\textbf{1.844 \%}$ and dominance of $\textbf{44.82 \%}$ on average for all the stocks. In terms of interpretation,
this means that linear regression is inferior, in terms of prediction accuracy, to the naïve benchmark on the given interval.

It is possible to analyze performance of the model for the given set of stocks. In Figure \ref{fig:3.5} is the overview of distributions of percentual deviations of predictions on the training-validation interval.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.70\linewidth]{images/distr_lr.png}
    \caption{\centering Errors in prediction for single stocks (linear regression).}
    \label{fig:3.5}
\end{figure}
In the vast majority of stocks, maximum error of $20 \%$ was observed; however even pathological predictions with abnormally high error was suspected. Even though, these cases were observed
in minimal number of cases – average percentual error per stock did not exceed $4 \%$. 

\section{Lasso regularization}
\label{sub:lasso}

Introducing regularization into the regression process might presumably improve the overall performance in terms 
of the introduced metrics, even though explicit feature selection has not proved to be sufficient. Apart from the linear regression framework
it is necessary to further tune the regularization parameter $\lambda$, together with the length of the training set $p$.

Figure \ref{fig:3.3} visualizes the performance of MAPE depending on the chosen parameters $\lambda$ and $p$ for the ENPH stock. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_lambda.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter and regularization parameter (stock ENPH).}
    \label{fig:3.3}
\end{figure}
In terms of the parameter $p$, it is possible to observe that the lowest MAPE is achieved in the approximate interval $p \in [4, 8]$ for the chosen values of $\lambda$.
Based on further evaluation of mean MAPE performance across all stocks, the lowest mean MAPE was achieved for $p = 4$. 

Further, the interval of the optimal setting of $\lambda$ for the given stocks is $\lambda \in [4, 5]$. Figure \ref{fig:3.4} depicts an overview of MAPE for the chosen ENPH stock.
What needs to be pointed out is the MAPE fluctuation on the given subinterval $[4.5, 4.7]$. The improvement remains negligible, reaching at most a magnitude of $10^{-3} - 10^{-2} \%$. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/lambda_lasso.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter and regularization parameter in Lasso (stock ENPH).}
    \label{fig:3.4}
\end{figure}
With the optimized parameters, we decided to verify two approaches to setting up the model. First, we used optimized values of $\lambda$ of each of the stocks. The resulting value 
is $\overline{\lambda}=4.15$. We set up the Lasso model with the given $\overline{\lambda}$ (General model) and, in contrast, we verified performance of a model that identifies the predicted stocks and applies its particular optimized $\lambda$
(Stock-wise model). The results of performance on the given training-validation interval
are summarized in Table \ref{tab:mape_lambda}.
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{Model Type} & \textbf{MAPE (\%)} & \textbf{Dominance (\%)} \\
\hline
General model & 2.297 & 36.16 \\
\hline
Stock-wise model & 2.27 & 35.81 \\
\hline
\end{tabular}
\caption{MAPE and dominance values for different settings of the model (Lasso).}
\label{tab:mape_lambda}
\end{table}
Generally speaking, compared to the linear regression, there is a marginal decline in terms of both metrics. What is interesting is the behaviour of both metrics. The General model has achieved comparatively worse MAPE; however,
its dominance is slightly higher than that of the Stock-wise model.

Lastly, Figure \ref{fig:3.6} visualizes the performance of the optimized model for individual stocks. The visualization shows performance of the General model
as there is no significant difference in the corresponding visualization of the Stock-wise model. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/distr_lasso.png}
    \caption{\centering Errors in prediction for single stocks (General model Lasso).}
    \label{fig:3.6}
\end{figure}
It is possible to suspect a very similar distribution to the one in Figure \ref{fig:3.5}, which visualizes distribution of error in linear regression.
Even so, there are a few minor differences – the order of the stocks, in terms of the range of errors, is not preserved. For example, the lowest range in Lasso was achieved on AAPL stock (Apple Inc.); however,
in linear regression, it was achieved for the IBM stock. This suggests that different ML algorithms most likely perform differently when predicting next-day price of single stocks – the stock having relatively the best prediction results
under one algorithm does not necessarily perform best under another.

\section{Ridge regularization}
Similarly to Lasso, there are several properties of Ridge regularization, that might improve the performance of the regression, compared to the
linear regression. The parameter tuning process in this case is analogous to the one used in Lasso (see Subchapter \ref{sub:lasso}).

In Figure \ref{fig:3.7}, the dependence of MAPE for the chosen ENPH stock on the selected regularization parameter $\lambda$ and the training window length $p$ is depicted.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/enph_heat_ridge.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter and regularization parameter in Ridge (stock ENPH).}
    \label{fig:3.7}
\end{figure}
Directly compared to the MAPE development in Figure \ref{fig:3.3}, there is clear evidence of the optimal value of $p$ being 
in the segment of $p \in [4, 6]$. The rise of error with the increasing value of $p$ is much more visible and stable for various values of $\lambda$.
With further experiments, value of $p=4$ was chosen for later analyses. Analogous to the problem of choosing value of $p$ in Lasso regression,
a certain subinterval of near-optimal values was localized – the lowest value was chosen to save computational time. 

As seen in Figure \ref{fig:3.8}, dependence of MAPE on the value of the parameter $\lambda$ tends to follow a convex course. The segment of optimal $\lambda$ for the set of stocks 
lies in the interval $\lambda \in [170, 260]$.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/lambda_ridge.png}
    \caption{\centering MAPE dependence on the rolling window training length parameter and regularization parameter in Ridge (stock ENPH).}
    \label{fig:3.8}
\end{figure}
Similar to the approach in Lasso regularization, using two model settings were chosen. The average value of optimal $\lambda$ for the analysed stocks 
is $\overline{\lambda}=211.85$ (General model). Similarly, a model using the optimal value of $\lambda$ for each stock independently was also implemented (Stock-wise model).
The final evaluation of these models is accumulated in Table \ref{tab:mape_lambda2}.
\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\textbf{Model Type} & \textbf{MAPE (\%)} & \textbf{Dominance (\%)} \\
\hline
General model & 1.791 & 45.08 \\
\hline
Stock-wise model & 1.79 & 45.15 \\
\hline
\end{tabular}
\caption{MAPE and dominance values for different settings of the model (Ridge).}
\label{tab:mape_lambda2}
\end{table}
Compared to the evaluation of linear regression, progress was achieved in both metrics progress. Ridge models have clearly better dominance
and MAPE. Interestingly, compared to the case of Lasso, where counter-intuitive behaviour of MAPE and dominance was observed, the opposite behaviour
is seen in Ridge models. General model tends to have higher MAPE and lower dominance, which is more intuitive.

Last but not least, an overview of error distributions for individual stocks in the models is depicted in Figure \ref{fig:3.9} (General model). Similar to the Lasso case, the distinctions between
General and Stock-wise models are negligible. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/distr_ridge.png}
    \caption{\centering Errors in prediction for single stocks (General model Ridge).}
    \label{fig:3.9}
\end{figure}
Overall, there are only minor distinctions to the presented Figures \ref{fig:3.5} and \ref{fig:3.6}, mainly in the order of stocks based on the error range. This again suggests
that different ML frameworks tend to have varying performance across different stocks.

\section{k-Nearest neighbours regression}
Compared to the methods used earlier in this chapter, k-NN is a non-parametric method. The effect of this property on the predictive performance
is definitely worth exploring. Although the framework seems simple and one might expect limited practical applicability, several studies demonstrate
its potential and relevance in predicting stock prices.

Huang \cite{clanok5} included the k-NN algorithm in the stack of algorithms applied. Even though k-NN did not prove to be the best-performing among the algorithms used, Huang
states that k-NN performs better in terms of the employed evaluation metric than more complex support vector regression framework.

Further, Yin et al. \cite{konferencia1} showed that the k-NN algorithm tends to perform well, as their analysis of certain US stocks demonstrated that the
deviation of predictions is relatively low, compared to other frameworks even for longer forecast periods.

In the case of this algorithm, it is necessary to analyze the combined effect of choosing both parameters $k$ (number of neighbours) and the length of the training window $p$.
Figure \ref{fig:3.10} shows the effect of dependence of MAPE on given values of parameters. The visualization is based on the averaged results of four given stocks: Roper Technologies Inc. (ROP),
Enphase Energy Inc. (ENPH), Cadence Design Systems Inc. (CDNS), and EPAM Systems Inc. (EPAM).
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/knn_heat.png}
    \caption{\centering Dependence of $k$ and $p$ parameters on MAPE of next-day predictions (k-NN; average among chosen stocks).}
    \label{fig:3.10}
\end{figure}
There is a clear trend showing that the most influential parameters is $k$, number of neighbours. The most interesting observation is the almost constant MAPE
across all the considered values of $p$. This appears to be highly counter-intuitive quality; however, further analyses have shown that it could be explained 
as a form of pathological behaviour of k-NN algorithm. 

Across all stocks k-NN tends to reach its optimal setting when $k = 1$. In all rolling window steps across all the stocks, the closest '{}neighbour'
to the prediction input is the observation from the most recent previous day. As k-NN has optimal setting in $k = 1$ regardless of $p$, k-NN algorithm effectively performs
as a form of the proposed naïve benchmark. Consequently, k-NN cannot outperform it. Such setting ($k=1, p=4$) has achieved average MAPE per stock $\textbf{1.69 \%}$.

To complete the analysis, Figure \ref{fig:3.11} depicts the distribution of errors across stocks. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{images/distr_knn.png}
    \caption{\centering Errors in prediction for single stocks (k-NN).}
    \label{fig:3.11}
\end{figure}
Again, the same property of model applies here as well. The individual error distributions tend to be very similar; however, the order of stocks based on range of the error is slightly shuffled. Interestingly,
the order of stocks is very similar to the one in Figure \ref{fig:3.9} (Ridge). This may root from the fact that these models are very close to each other in terms of MAPE per stock. 


\section{Random forest regression}
% Pre čitateľnosť, keď píšem v pdf-ku
\newpage

\section{ARIMA regression}

